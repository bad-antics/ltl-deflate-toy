// examples/demo.ltl — runnable demo for ltl_deflate_toy

import std::io
use ltl_deflate_toy as lib

fn main() {
    io::println("ltl_deflate_toy demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}