// ltl-deflate-toy/main.ltl
//
// DEFLATE dynamic-Huffman encoder compatible with gzip.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-deflate-toy v0.1.0")
    io::println("DEFLATE dynamic-Huffman encoder compatible with gzip.")
    0
}
